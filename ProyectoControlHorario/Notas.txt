 No, no deberías guardar la contraseña en texto plano, ni en 
SQLite, ni en MySQL, ni en PostgreSQL, ni en ninguna base de datos.
 Da igual el tipo de base de datos — la contraseña siempre debe 
guardarse de forma cifrada (en realidad, “hasheada”). En Java (con 
Spring Boot), se usa normalmente BCrypt.


  SQLite no es un servidor como PostgreSQL o MySQL, sino un fichero 
en disco.
  SQLite garantiza la integridad del fichero, pero no garantiza la 
correcta ejecución concurrente de tus operaciones.

  Exacto 💡 — no es lo más recomendable usar JdbcTemplate con SQLite en un 
escenario como el que describes (múltiples hilos, varias bases de datos, operaciones concurrentes).

   JdbcTemplate es una clase de Spring que:
      Abstrae el manejo de conexiones JDBC (abre, ejecuta, cierra).
      Está pensada para bases de datos servidoras (MySQL, PostgreSQL, Oracle, etc.).
      Asume que hay un pool de conexiones (HikariCP, por ejemplo) que gestiona concurrencia por ti.

   En otras palabras, JdbcTemplate está diseñada para entornos donde:
      Varias conexiones independientes pueden coexistir sin interferirse.

   El archivo 'schema.sql' define la estructura común para todas tus bases SQLite. Spring no lo 
ejecutará automáticamente, pero tu clase DatabaseInitializer lo leerá y aplicará a cada base.

   mvn clean
   mvn spring-boot:run
